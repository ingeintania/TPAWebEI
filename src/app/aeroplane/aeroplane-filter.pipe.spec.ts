import { AeroplaneFilterPipe } from './aeroplane-filter.pipe';

describe('AeroplaneFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AeroplaneFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
