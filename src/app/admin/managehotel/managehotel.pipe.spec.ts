import { ManagehotelPipe } from './managehotel.pipe';

describe('ManagehotelPipe', () => {
  it('create an instance', () => {
    const pipe = new ManagehotelPipe();
    expect(pipe).toBeTruthy();
  });
});
