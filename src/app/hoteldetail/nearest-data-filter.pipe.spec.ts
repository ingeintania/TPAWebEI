import { NearestDataFilterPipe } from './nearest-data-filter.pipe';

describe('NearestDataFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new NearestDataFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
